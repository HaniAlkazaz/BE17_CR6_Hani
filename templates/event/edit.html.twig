{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}
<div class= "container"style="background-color: #b0c2e6fd">

    
    <h4 class="text-md-center h1">Edit Event {{ event.name }}</h4>
    {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_event_index') }}"class= "btn btn-success mt-2">back to list</a>

    {{ include('event/_delete_form.html.twig') }}
    

    </div>
{% endblock %}
